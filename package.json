{
  "name": "speak2mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server: Speak2 transcript tail + Bee CLI (facts/todos) for live operator steering",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "speak2mcp": "./src/server.mjs"
  },
  "scripts": {
    "start": "node ./src/server.mjs",
    "test": "node --test ./tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
